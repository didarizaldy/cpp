#include <stdio.h>
#include <iostream>
#include <stdlib.h>
#include <time.h>
int main()
{
    int ulangi;
    {
        int i;
        srand((unsigned)time(NULL));
        
        printf(" %d\n", i = (rand() % 99));
		for (i != 88 && i < 88)
		{
			printf("Hasil bilangan yang di random : \n");
		};
}
